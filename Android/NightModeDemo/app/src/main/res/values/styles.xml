<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Creating a Custom reusable style for Image Buttons -->
    <!-- Style for all the Image Buttons to have Circle Background  -->
    <style name="CounterButton" parent="TextAppearance.AppCompat.Button">
        <item name="android:background">@drawable/circle_drawable</item>
        <item name="android:tint">@color/colorPrimary</item>
        <item name="android:layout_marginLeft">10dp</item>
        <item name="android:layout_marginRight">10dp</item>
    </style>

    <!-- Style for Decrease Counter Button -->
    <style name="MinusCounter.Button.Style" parent="CounterButton">
        <item name="android:src">@drawable/ic_minus</item>
        <item name="android:contentDescription">@string/decrease_counter</item>
    </style>

    <!-- Style for Increase Counter Button -->
    <style name="PlusCounter.Button.Style" parent="CounterButton">
        <item name="android:src">@drawable/ic_plus</item>
        <item name="android:contentDescription">@string/increase_counter</item>
    </style>

    <!-- Header Style -->
    <style name="CounterHeader" parent="TextAppearance.AppCompat.Headline">
        <item name="android:color">@color/colorPrimary</item>
    </style>

    <!-- Style for the Counter Value -->
    <style name="CounterValueStyle" parent="TextAppearance.AppCompat.Display1">
        <item name="android:textColor">@android:color/holo_red_dark</item>
        <item name="android:textSize">@dimen/counter_size</item>
        <item name="android:textStyle">bold</item>
    </style>

</resources>